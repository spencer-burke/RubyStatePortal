<%= turbo_frame_tag "modal" do %>
    <dialog open>
        <article>
          <%= form_with model: single_unit_property, url: properties_path do |f| %>
            <%= f.hidden_field :type, value: "SingleUnitProperty" %>

            <label>
                Name
                <%= f.text_field :name, required: true %>
            </label>

            <label>
                Address
                <%= f.text_field :address, required: true %>
            </label>

            <label>
                Unit/Apt #
                <%= f.text_field :address_extension %>
            </label>

            <div class="grid">
            <label>
                City
                <%= f.text_field :city, required: true %>
            </label>

            <label>
                State
                <%= f.text_field :state, required: true %>
            </label>
            </div>

            <label>
                Country
                <%= f.text_field :country, required: true %>
            </label>

            <footer>
              <%= f.submit "Create Single Unit Property", role: "button" %>
              <button type="button" data-action="click->manager-dashboard#closeModal" class="secondary">
                  Cancel
              </button>
            </footer>
          <% end %>
        </article>
    </dialog>
<% end %>
